<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.breezelab.sample.dao.UserDAO">
    <select id="getTestData" resultType="com.breezelab.sample.dto.UserDTO">
        # user 테이블을 바라보지 못한다.. 계속 같은 문제;
        SELECT * FROM user
    </select>
</mapper>
